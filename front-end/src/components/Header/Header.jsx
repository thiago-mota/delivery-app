import React from 'react';
// import PropTypes from 'prop-types';

const Header = () => {
  return (
    <header>
      Hello World
    </header>
  );
};

// Header.propTypes = {
  
// };

export default Header;
